-r requirements.txt
# Python developer/test tooling installed by:
#   python kindlemaster.py bootstrap
pytest>=8.4.0
coverage[toml]>=7.15.0
ruff>=0.15.20
pip-audit>=2.7.0
# Optional local verification surfaces:
# - browser: Playwright Python package plus a separate Chromium install
# - runtime: Waitress plus the browser surface toolchain
playwright>=1.61.0
waitress>=3.0.2
scikit-learn>=1.9.0
