name: Apply issue 322 Unicode normalization

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write
  pull-requests: read

jobs:
  apply:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Apply Unicode normalization fix
        run: python scripts/patch_issue_322_unicode_normalization.py

      - name: Remove one-time patch machinery
        run: rm scripts/patch_issue_322_unicode_normalization.py .github/workflows/apply-issue-322-unicode-normalization.yml

      - name: Commit normalization fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --cached --check
          git commit -m "Normalize non-decomposing identity characters (#322)"
          git push origin HEAD:${{ github.head_ref }}
