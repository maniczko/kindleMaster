# Phase 11 Continuation Plan

1. Execute `FX-003` as a focused follow-on remediation for the image-heavy mixed-layout chess corpus using the completed Phase 8 evidence and decision map.
2. Extend input coverage with book-like and magazine-like fixtures, then re-run baseline and regression scans across the broader corpus.
3. Re-run technical validation with stronger external validation coverage and close `ISSUE-012` only when that evidence is stored.
4. Recompute the release score and re-open `T11-001` if blockers are cleared.
